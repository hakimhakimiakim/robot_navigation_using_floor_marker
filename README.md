# robot_navigation_using_floor_marker
"This repository contains code for line detection and fiducial marker detection, combined into a single case study. It also includes a summary file, training datasets (line & marker), and testing datasets (line & marker) for documentation and experimental purposes.
